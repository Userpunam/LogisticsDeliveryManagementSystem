namespace LogisticsDeliveryManagementSystem.Models
{
    public enum PackageStatus
    {
        Pending,
        OutForDelivery,
        Delivered
    }

    public enum RouteStatus
    {
        Pending,
        InProgress,
        Completed
    }
}
